const Add = (a, b) => {
  return a + b;
};

export default Add;

export const ReverseString = (str) => {
  let revstr = "";
  for (let i = str.length - 1; i >= 0; i--) {
    revstr += str[i]
  }
  return revstr;
};

